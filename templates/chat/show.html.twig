{% extends 'base.html.twig' %}

{% block title %}Chat{% endblock %}

{% block body %}
    <h1>Chat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chat.id }}</td>
            </tr>
            <tr>
                <th>IdUsuario1</th>
                <td>{{ chat.idUsuario1 }}</td>
            </tr>
            <tr>
                <th>IdUsuario2</th>
                <td>{{ chat.idUsuario2 }}</td>
            </tr>
            <tr>
                <th>Asunto</th>
                <td>{{ chat.asunto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chat_index') }}">back to list</a>

    <a href="{{ path('chat_edit', {'id': chat.id}) }}">edit</a>

    {{ include('chat/_delete_form.html.twig') }}
{% endblock %}

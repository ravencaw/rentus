{% extends 'base.html.twig' %}

{% block title %}Edit Foto{% endblock %}

{% block body %}
    <h1>Edit Foto</h1>

    {{ include('foto/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('foto_index') }}">back to list</a>

    {{ include('foto/_delete_form.html.twig') }}
{% endblock %}

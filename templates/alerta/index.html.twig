{% extends 'base.html.twig' %}

{% block title %}Alerta index{% endblock %}

{% block body %}
    <h1>Alerta index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Id_inmueble</th>
                <th>Id_usuario</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for alertum in alertas %}
            <tr>
                <td>{{ alertum.id }}</td>
                <td>{{ alertum.idInmueble }}</td>
                <td>{{ alertum.idUsuario }}</td>
                <td>
                    <a href="{{ path('alerta_show', {'id': alertum.id}) }}">show</a>
                    <a href="{{ path('alerta_edit', {'id': alertum.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('alerta_new') }}">Create new</a>
{% endblock %}

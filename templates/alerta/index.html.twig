{% extends 'base_back.html.twig' %}

{% block title %}Mis alertas{% endblock %}

{% block body %}
    <h1>Mis alertas</h1>

    <ul style="list-style:none">
        {% for alerta in alertas %}
            <li>
                <div class="card mb-3 col-md-12">
                    <div class="row no-gutters">
                        <div class="col-md-6">
                            {% if fotos[inmuebles[alerta.id].id] %}
                                <img src="{{ asset('img/inmuebles/')  }}{{ inmuebles[alerta.id].id }}/{{ fotos[inmuebles[alerta.id].id].ruta}}" class="card-img" alt="...">
                            {% else %}
                                <img src="{{ asset('img/no_image.jpg')  }}" class="card-img" alt="...">
                            {% endif %}
                        </div>
                        <div class="col-md-6">
                        <div class="card-body">
                            <h5 class="card-title">{{ inmuebles[alerta.id].direccion }}</h5>
                            <p class="card-text">Precio: {{ inmuebles[alerta.id].precio }} €</p>
                            <p class="card-text">{{ inmuebles[alerta.id].ciudad }} {{ inmuebles[alerta.id].cp }}</p>
                            <p class="card-text"><small class="text-muted">{{ inmuebles[alerta.id].superficie }} m<sup>2</sup> - {{ (inmuebles[alerta.id].precio/inmuebles[alerta.id].superficie)|round(2,'floor') }} €/m<sup>2</sup></small></p>
                            <a class="btn btn-danger col-md-12" href="{{ path('alerta_delete', {'id': alerta.id, 'idInmueble': alerta.idInmueble}) }}"><i class="icon ion-close"></i></a>
                        </div>
                        </div>
                    </div>
                </div>
            </li>
        {% else %}
            <li>
                <span colspan="4">No existen alertas programadas</span>
            </li>
        {% endfor %}
        </ul>
{% endblock %}

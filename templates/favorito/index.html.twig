{% extends 'base.html.twig' %}

{% block title %}Favorito index{% endblock %}

{% block body %}
    <h1>Favorito index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Id_usuario</th>
                <th>Id_inmueble</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for favorito in favoritos %}
            <tr>
                <td>{{ favorito.id }}</td>
                <td>{{ favorito.idUsuario }}</td>
                <td>{{ favorito.idInmueble }}</td>
                <td>
                    <a href="{{ path('favorito_show', {'id': favorito.id}) }}">show</a>
                    <a href="{{ path('favorito_edit', {'id': favorito.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('favorito_new') }}">Create new</a>
{% endblock %}
